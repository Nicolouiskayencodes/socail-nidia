const authentication = require('./authentication')
const user = require('./userController')
const post = require('./postController')
const comment = require('./commentController')

module.exports = {authentication, user, post, comment}